# Talksy Mobile Frontend Environment Variables
# Copy this file to '.env' and fill in your values.

# The API entry point. 
# Note: When run on physical devices (Android/iOS emulators or Expo Go),
# the code dynamically replaces 'localhost' with your hosting machine's local network IP.
EXPO_PUBLIC_API_URL=http://localhost:5000/api
